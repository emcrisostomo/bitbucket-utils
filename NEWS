NEWS
****

New in 1.2.1:

  * Fix a bug that prevents bb to correctly process BitBucket API responses
    containing new line characters in a literal JSON string.

New in 1.2.0:

  * Allow anonymous BitBucket API calls.
  * Maintain per-user reponse cache.

New in 1.1.0:

  * Maintain a local cache of BitBucket API responses to speed up multiple
    invocations.

New in 1.0.0:

  * List repositories.
  * List repository URLs.
  * Build package using GNU Autotools.
